/*import React, { Component } from 'react';

import { Calendar, notification } from 'antd';

export default class CalendarF extends Component{
    onSelect = (value) => {
        notification.open({
            message: 'You selected date',
            description: value.format('DD-MM-YYYY'),
            duration: 3,
            placement: 'bottomRight',
        });
    };
    render(){
        return(
            <Calendar  onSelect={this.onSelect}/>
        )
    }
}*/

